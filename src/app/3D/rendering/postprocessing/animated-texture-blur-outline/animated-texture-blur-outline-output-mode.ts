/**
 * Used for debug purposes to show the texture that is used as the outline.
 */
export enum AnimatedTextureBlurOutlineOutputMode {
  // The normal shader output so hover or selected masked by the object.
  Normal = 0,
  HoverTexture = 1,
  SelectedTexture = 2,
}
